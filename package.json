{
  "name": "hardhat-project",
  "devDependencies": {
  },
  "scripts": {
    "clean": "npx rimraf ./dist/**/*.js  ./dist/*.js",
    "deploy:dev": "npm run clean&&npx hardhat compile&&npm install --production&&tsc&&pm2 start  pm2.config.js",
    "deploy:prod": "npm run clean&&npx hardhat compile&&npm install --production&&tsc&&pm2 start  pm2.config.js --env production  "
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.1",
    "@openzeppelin/contracts": "^4.8.2",
    "mysql2": "^3.2.0",
    "pg-promise": "^11.4.3",
    "rimraf": "^5.0.0"
  }
}
